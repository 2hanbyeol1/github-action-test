name: Github-Action-Test

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  # workflow_dispatch:

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # Github Repository Secrets를 통해 환경 변수 파일을 생성합니다.
      - name: Generate Environment Variables File
        run: |
          echo "VITE_ENV_TEST=$VITE_ENV_TEST" >> .env.local
          echo "VITE_ENV_TEST is $VITE_ENV_TEST"
        env:
          VITE_ENV_TEST: ${{ secrets.VITE_ENV_TEST }}
      - name: Check Lint
        run: yarn lint
      - name: Delete ./out & Build
        run: |
          rm -rf ./out
          yarn build
      - name: Connect with remote server
        run: echo "SSH - Connect with remote server"
      - name: Copy files to remote server
        run: echo "SCP - Copy files to remote server"
